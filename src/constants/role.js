const ROLES = {
    ADMIN: 'admin',
    USER: 'user',
    SELLER: 'seller'
};

const ROLE_LIST = [ROLES.ADMIN, ROLES.USER, ROLES.SELLER];

const SIGNUP_ROLES = [ROLES.USER, ROLES.SELLER];

module.exports = {
    ROLES,
    ROLE_LIST,
    SIGNUP_ROLES
};